pip -m venv <name of venv>
select .py file for vscode can autodetect or choose environment -> python venv at the bottom command
run PS command Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser to activate venv python
cd to name_of_venv/Scripts -> run cmd activate
setup pip packages = pip install packages name...
choose AI (virtual env folder->open new intergrated terminal)